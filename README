Basic Rails application

1) Create the app

$ mkdir inventory
$ rails new inventory
$ cd inventory
$ git init

2) Create the database

$ rails g migration create_items
$ rails g migration create_suppliers

* edit migrations *

$ rake db:migrate

3) Interact with the application

$ rails c
$ rails db
$ rails s

4) Extras

View all available routes

$ rake routes
